---
import "../styles/global.css";
import LeftPane from "../components/LeftPane.astro";
import RightPane from "../components/RightPane.astro";
import { navItems, profile } from "../lib/site";

const { activePath, pageTitle, pageDescription } = Astro.props;
---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={pageDescription} />
    <title>{pageTitle}</title>
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
  </head>
  <body class="bg-black text-zinc-50 antialiased">
    <div class="mx-auto grid min-h-screen max-w-7xl lg:grid-cols-[260px_minmax(0,1fr)] xl:grid-cols-[260px_minmax(0,1fr)_340px]">
      <LeftPane activePath={activePath} />
      <main>
        <header class="border-b border-zinc-800 px-4 py-4 lg:hidden">
          <div class="flex items-center justify-between gap-3">
            <div class="flex items-center gap-3">
              <img src={profile.avatar} alt={profile.name} class="h-12 w-12 shrink-0 rounded-full object-cover" />
              <div>
                <p class="text-base font-bold text-zinc-100">{profile.name}</p>
                <p class="text-xs text-zinc-400">{profile.handle}</p>
                <p class="text-xs text-zinc-400">{profile.role}</p>
              </div>
            </div>
            <div class="relative">
              <button id="menu-toggle" class="flex h-10 w-10 items-center justify-center rounded-full text-zinc-400 hover:bg-zinc-900 hover:text-zinc-100" aria-label="Menu">
                <svg id="menu-icon" class="h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
                </svg>
                <svg id="close-icon" class="hidden h-6 w-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
                </svg>
              </button>
              <div id="menu-dropdown" class="absolute right-0 top-12 z-50 hidden w-48 rounded-xl border border-zinc-800 bg-zinc-950 p-2 shadow-xl">
                <a href="/contact" class="block rounded-lg px-4 py-3 text-sm font-medium text-zinc-300 hover:bg-zinc-900 hover:text-zinc-100">Contact</a>
                <a href="/customize" class="block rounded-lg px-4 py-3 text-sm font-medium text-zinc-300 hover:bg-zinc-900 hover:text-zinc-100">Customize</a>
              </div>
            </div>
          </div>
          <p class="mt-3 text-sm leading-relaxed text-zinc-300">{profile.bio}</p>
        </header>
        <slot />
      </main>
      <RightPane />
    </div>
    <nav class="fixed inset-x-0 bottom-0 border-t border-zinc-800 bg-black/95 px-2 py-2 backdrop-blur lg:hidden">
      <ul class="grid gap-2" style="grid-template-columns: repeat(5, minmax(0, 1fr));">
        {navItems.filter(item => !item.desktopOnly).map((item) => (
          <li>
            <a
              href={item.href}
              class:list={[
                "block rounded-full px-3 py-2 text-center text-xs font-medium",
                activePath === item.href
                  ? "bg-sky-500 text-white"
                  : "text-zinc-300 hover:bg-zinc-900/80 hover:text-zinc-100"
              ]}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
    <script is:inline>
      const toggle = document.getElementById("menu-toggle");
      const dropdown = document.getElementById("menu-dropdown");
      const menuIcon = document.getElementById("menu-icon");
      const closeIcon = document.getElementById("close-icon");

      if (toggle && dropdown) {
        toggle.addEventListener("click", (e) => {
          e.stopPropagation();
          dropdown.classList.toggle("hidden");
          menuIcon.classList.toggle("hidden");
          closeIcon.classList.toggle("hidden");
        });

        document.addEventListener("click", (e) => {
          if (!dropdown.classList.contains("hidden") && !dropdown.contains(e.target) && !toggle.contains(e.target)) {
            dropdown.classList.add("hidden");
            menuIcon.classList.remove("hidden");
            closeIcon.classList.add("hidden");
          }
        });
      }
    </script>
  </body>
</html>
