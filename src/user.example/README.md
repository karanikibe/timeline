# Example User Content

This directory contains safe placeholder content for the open-source repo.

To start customizing, copy this directory to `src/user/`:

- `cp -R src/user.example src/user`

Or run the helper script:

- `npm run init:user`

