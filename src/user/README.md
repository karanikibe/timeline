# User Content

Everything in `src/user/` is intended to be personalized.

To customize this site without touching the code:
- Edit `src/user/site.ts`
- Add/edit builder log entries in `src/user/content/builder-log/`

If you want a clean reset, copy from `src/user.example/`.

